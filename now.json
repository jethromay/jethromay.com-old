{
  "build": {
    "env": {
      "MAILCHIMP_URL": "@mailchimp-url"
    }
  },
  "env": {
    "MAILCHIMP_URL": "@mailchimp-url"
  },
  "builds": [{ "src": "package.json", "use": "@now/next" }],
  "routes": [
    { "src": "/posts/(?<page>[^/]+)$", "dest": "/posts?page=$page" }
  ]
}
